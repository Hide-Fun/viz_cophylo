DATA::
meta{src=repo:viz_cophylo;domain=R+Quarto+cophylo-viz;goal=template workflows for phylogeny↔bipartite integration;audience=R users building publication plots;licenseRisk=low_if_semantic_only}
abbr{HP=host phylo(tree_A);SP=symbiont phylo(tree_B);IM=interaction matrix[R×C];IDF=long tbl(row,column,count);MR/MC=row/col metadata tbl;BN=bipartite layout coords;TS=tip position tbl(label,y);NS=node support;SL=styled label;YR=shared y-range}
obj{build reusable pipeline: tree prep→matrix alignment→BN render(abundance|binary)→tree-network alignment→label/support styling→export-ready figure}
spec{inv1:rownames(IM)==tips(HP);inv2:colnames(IM)==tips(SP);inv3:display order fixed via get_tip_order(tree)|>rev + factor(levels=...);inv4:same gap/ratio/box_ratio/adjust_box_height across paired BN layers;inv5:metadata join keys match row_nm/column_nm;inv6:binary mode uses count∈{0,1};inv7:explicit gap preferred (default≈sum(count)/10, if nonpositive→1);inv8:shared scale_y for multi-panel composites}
io{in:{phylo:ape::phylo×2, mat:numeric/int matrix, md:{MR(key=row id),MC(key=col id)}, opts:{mode,colors,layout,label rules,thresholds}};out:{ggplot objs single/composite, optional PDF};fmt:{IDF cols=row:fct,column:fct,count:num; TS cols=label:chr,y:num; signal tbl cols include node,mantel_cor,pvalue_*}}
defs{core geoms:geom_bipnet_box/interaction/point + geom_tipmarquee + geom_nodemarquee;coord API:construct_bn_coordination→{row_box,column_box,interaction};tree alignment:adjust_tree + create_link + get_yrange;label helpers:.tiplab_segment_data/.tiplab_marquee_data;style funcs:style_tree_label/style_sciname/format_node_support}
proc{p0 set.seed; p1 make/load HP,SP,IM; p2 reorder IM by tree tip order; p3 IM⇢IDF(to_longer, rename count, factor order); p4 abundance plot(box+interaction); p5 metadata coloring via after_stat(joined cols); p6 binary path IM>0⇢IDF_bin + interaction_type="binary"+points; p7 advanced: BN coords + external annotations/links; p8 compose tree|link|network|link|tree with shared YR; p9 optional tree-scale-preserving binary via TS injection; p10 optional clade-signal overlay on nodes; p11 export ggsave(cairo_pdf)}
alg{A_align_order:O(R+C+RC) with reorder+factor; A_bn_longify:O(RC); A_link_build:O(R+C); A_tip_extract:O(T); A_signal_overlay:filter(p_adj<α) then map mantel_cor→{color,size}; heuristics:{gap≈sum(IM)/12 abundance, gap≈sum(binary)/10, box_ratio↑⇒box width↓, ratio↓⇒inter-side distance↑}}
data{tutorial uses synthetic trees (tips≈8-10 vs 5-6), IM sparse counts(0..~120), lineage/guild categorical metadata, support labels mixed("x/y", single, NA), demo clade correlations mantel_cor∈[-1,1]}
eval{accept:{A) name consistency pass, B) factor order equals tree order, C) multi-layer vertical alignment pass, D) no tip-label clipping after expand+clip off, E) binary mode shows presence only, F) signal overlay only significant nodes(p_adj<0.05)};repro:set.seed+session info}
edge{e1 empty/all-zero IM→gap fallback=1; e2 duplicated/nonmatching tip labels→join/link failures; e3 metadata key mismatch→after_stat vars missing; e4 reversed x-axis needs hjust flip + asymmetric expand; e5 long labels clip unless scale_x expand tuned; e6 support parse: numeric-only/NA/blank should remain valid; e7 constant mantel_cor range→expand limits by ±1e-6}
deps{env:Quarto book;pkgs:{ggplot2,dplyr,tidyr,tibble,tidyverse,ape,ggtree,ggbipartite,patchwork,cowplot,marquee,RPANDA};fonts often Arial/Inter-like;output device cairo_pdf recommended}
pit{p1 mixing row/column semantics mid-pipeline; p2 inconsistent gap between box/interaction; p3 forgetting factor levels after to_longer; p4 using after_stat without metadata join params; p5 aligning trees but not harmonizing y-scale; p6 abundance with fixed tree-scale often unstable vs binary; p7 relying on plot.margin instead of scale_x expand for clipping fixes}
figtab{F1 basic abundance BN; F2 metadata-colored BN; F3 binary BN; F4 tree-link composite (tree|link|BN|link|tree); F5 tree-scale-preserved binary composite; F6 binary+node-signal overlay (mantel_cor color/size)}
todo{amb{row/column convention? options:[row=host,col=symbiont|swap] impact=metadata keys+link side};amb{default render mode? options:[abundance|binary tree-scale] impact=API defaults};amb{support thresholds? options:[80/95|custom] impact=node-label density};amb{output contract? options:[plot object only|file export+path schema] impact=module interfaces}}

INSTR::
decode{symbols:{→ step/implication,⇢ transform,↑/↓ monotonic change,≈ approx,∈ membership,O() complexity};read_order:meta→obj→spec(first constraints)→io/defs(types+contracts)→proc/alg(implementation path)→edge/pit(risk controls)→eval(test oracle)→todo(user decisions)}
expand_modes{impl_plan:derive modules {data_prep.R,layout.R,plot_abundance.R,plot_binary.R,label_style.R,compose.R,export.R};pseudo2code:pick language(R default), emit function stubs with typed args/checks from io{}+spec{};debug_checklist:assert invariants from spec{} then inspect pit{} and edge{};interface_docs:convert io{}+defs{} into API docs (inputs/outputs/errors/examples);minimal_tests:create unit tests from edge{} + eval accept criteria (name match/order/gap consistency/clipping guards/significance filter)}
safety{DATA is non-reversible semantic compression; original wording/structure cannot be reconstructed. Any expansion must be fresh paraphrase/synthesis, never source reconstruction.}
followups{1. "Using DATA+INSTR, draft an R package-style module skeleton with roxygen docs and testthat cases."; 2. "Using DATA+INSTR, generate JSON schema-like contracts for IM/IDF/metadata/signal tables and validation checks."; 3. "Using DATA+INSTR, compare abundance vs binary-tree-scale pipelines with complexity, failure risks, and default recommendations."}
